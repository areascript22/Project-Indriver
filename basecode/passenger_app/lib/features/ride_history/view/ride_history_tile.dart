import 'package:flutter/material.dart';

class RideHistoryTile extends StatelessWidget {
  const RideHistoryTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("Ride history tile"),
    );
  }
}
